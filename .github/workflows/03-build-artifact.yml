name: CI + Multi Build Workflow

on:
  workflow_dispatch:

env:
  IMAGE_NAME: tiny-health-app
  ARTIFACT_NAME: tiny-health-app-image

jobs:
  run-tests:
    name: CI - Run tests
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run tests
        run: python -m unittest discover -s tests -v

  build-artifact:
    name: Build Artifact (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: run-tests

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set image tag
        id: image-tag
        run: |
          IMAGE_TAG="py${{ matrix.python-version }}-run-${GITHUB_RUN_NUMBER}"
          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        run: |
          docker build \
            -t "${IMAGE_NAME}:${{ steps.image-tag.outputs.image_tag }}" \
            .

      - name: Save Docker image
        run: |
          docker save "${IMAGE_NAME}:${{ steps.image-tag.outputs.image_tag }}" \
          -o "image-${{ matrix.python-version }}.tar"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}-py${{ matrix.python-version }}
          path: image-${{ matrix.python-version }}.tar
          retention-days: 7